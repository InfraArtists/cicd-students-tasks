apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: go-web
  namespace: dev
  labels:
    name: go-web
    instance: go-web
spec:
  ingressClassName: nginx
  rules:
    - host: "go-dev.salehi.local"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: go-web
                port:
                  number: 80