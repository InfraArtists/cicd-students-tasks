
test_producer:
  stage: test
  image: python:3.11
  script:
    - cd reza_saadati/producer/tests
    - pip install -r requirements.txt
    - pip install pytest
    - pytest -v --maxfail=1 --disable-warnings